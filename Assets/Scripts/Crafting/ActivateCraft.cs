using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ActivateCraft : MonoBehaviour
{
    
    public GameObject inventory;
    // Start is called before the first frame update
   /*
    void Update()
    {
        if(Input.GetKeyDown(KeyCode.T)){
            inventory.GetComponent<InventoryUI>().UpdateUI();
            inventory.SetActive(!inventory.activeSelf);
            if(inventory.activeSelf) Time.timeScale = 0; else Time.timeScale = 1;
        }
        
        Cheating:
        if(Input.GetKeyDown(KeyCode.C)){
             inventory.GetComponent<InventoryUI>().inventory.AddSupply("alcohol", 6);
             inventory.GetComponent<InventoryUI>().inventory.AddSupply("rags", 5);
             inventory.GetComponent<InventoryUI>().inventory.AddSupply("sugar", 4);
             inventory.GetComponent<InventoryUI>().inventory.AddSupply("canisters", 3);
             inventory.GetComponent<InventoryUI>().inventory.AddSupply("bile", 2);
             inventory.GetComponent<InventoryUI>().inventory.AddSupply("gunpowder", 2);
        }
    }*/
}
